[Unit]
Description=a service that will setup docker things that are necessary to run the server

[Service]
Type=oneshot
ExecStart=/usr/bin/bash /.pickcel/scripts/docker.sh

[Install]
WantedBy=multi-user.target